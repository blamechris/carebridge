{
  "name": "@carebridge/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "phi:readiness:static": "tsx src/phi-readiness-check.ts --mode=static",
    "phi:readiness:runtime": "tsx src/phi-readiness-check.ts --mode=runtime"
  },
  "dependencies": {
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
